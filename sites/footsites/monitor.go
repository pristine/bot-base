package footsites

import (
	"github.com/EdwinJ0124/bot-base/internal/task"
)

func waitForMonitor(t *task.Task, internal *FootsitesInternal) task.TaskState {
	// wait for data to come from montitor

	// data := <- t.MonitorData

	// monitorData := data.(*footsites.FootsitesMonitorData)

	// handle data

	return ADD_TO_CART
}
